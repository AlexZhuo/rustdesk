name: Flutter Nightly Build x64 only

on:
  workflow_dispatch:

jobs:
  run-flutter-nightly-build-x64:
    uses: ./.github/workflows/flutter-build-x64.yml
    secrets: inherit
    with:
      upload-artifact: true
      upload-tag: "nightly"
